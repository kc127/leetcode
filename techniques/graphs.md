1. **DFS** (applies when you need to visit all nodes)
  - Preorder (Root -> Left -> Right)
  - Inorder (Left -> Root -> Right)
  - Postorder (Left -> Right -> Root)
  
2. **BFS** (shortest path)
  - Level Order
